[deploy]
# Start the HTTP MCP server using uvicorn directly (Railway sets $PORT)
startCommand = "uvicorn mcp_private_db.main:app --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
